// @flow

type t = {
  a: number,
};

function fn(a: string, b: number) {
  const c = (a: string);
  return parseInt(a) + b;
}

switch (true) {
  case true: {
    console.log("true");
  }
}

fn(1, 1);

fn("9", 3);